{
  "name": "jobman",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"api,web\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "lint": "oxlint --config oxlint.json frontend/src backend",
    "lint:fix": "oxlint --fix --config oxlint.json frontend/src backend",
    "format": "biome format frontend/src backend/*.ts",
    "format:fix": "biome format --write frontend/src backend/*.ts",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "tsc": "npm run tsc --prefix backend && npm run tsc --prefix frontend",
    "deploy:backend": "npm run deploy --prefix backend",
    "deploy:frontend": "npm run deploy --prefix frontend"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "concurrently": "^8.2.2",
    "oxlint": "^0.16.6"
  }
}
